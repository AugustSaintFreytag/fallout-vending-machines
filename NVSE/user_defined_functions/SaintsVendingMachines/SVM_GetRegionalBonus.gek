begin function {}
	
	array_var aRegionStockBonus = Ar_Construct "stringmap"

	string_var sRegionName = (GetEditorID Player.GetParentWorldspace)

	if sRegionName == "MegatonWorld"
		let aRegionStockBonus["Nuka-Cola"] := 1
	endif

	SetFunctionValue aRegionStockBonus

end